- peak hour start and peak hour end flags, only work with time series data. we can
  improve this. maybe even weekday / weekend schedules

- `simulate inspect-data` or `simulate inspect-columns` command so you can see valid
  weight-column candidates directly from the CLI instead of checking CSVs manually.

- saving to directories and folders